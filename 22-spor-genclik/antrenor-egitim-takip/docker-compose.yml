version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:80"
      - "5001:443"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=db;Database=AntrenorEgitimTakipDB;User=sa;Password=YourStrong@Passw0rd;TrustServerCertificate=true
    depends_on:
      - db
    volumes:
      - ./AntrenorEgitimTakip.API/wwwroot:/app/wwwroot
    networks:
      - antrenor-network

  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=YourStrong@Passw0rd
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql
    networks:
      - antrenor-network

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - antrenor-network

volumes:
  sqlserver_data:
  redis_data:

networks:
  antrenor-network:
    driver: bridge 